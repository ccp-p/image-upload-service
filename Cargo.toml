[package]
name = "hash-cdn"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "hash-cdn"
path = "main_new.rs"

[dependencies]
# Pure std-lib: zero external crates for minimal binary size on low-spec devices
